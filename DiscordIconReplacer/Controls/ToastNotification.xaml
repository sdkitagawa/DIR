<UserControl x:Class="DiscordIconReplacer.Controls.ToastNotification"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             HorizontalAlignment="Center"
             VerticalAlignment="Center"
             Margin="0"
             IsHitTestVisible="False"
             Opacity="0">
    <Border Background="{DynamicResource ToastBackground}"
            BorderBrush="{DynamicResource ToastBorder}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="16,10"
            HorizontalAlignment="Center">
        <TextBlock x:Name="MessageText" Foreground="{DynamicResource TextPrimary}" FontSize="13"/>
    </Border>
</UserControl>